{
  "name": "oen-digitals-monorepo",
  "packageManager": "pnpm@10.12.4",
  "engines": {
    "node": "20.x"
  },
  "private": true,
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  },
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build && pnpm -r --filter ./oen-digitals/artifacts/... run build && pnpm -r --filter ./oen-digitals/lib/... run build && pnpm -r --filter ./oen-digitals/scripts run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.100.9"
  },
  "devDependencies": {
    "typescript": "~5.9.2",
    "prettier": "^3.8.1",
    "vite": "^8.0.12"
  }
}
