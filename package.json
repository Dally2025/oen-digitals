{
  "name": "oen-digitals-monorepo",
  "packageManager": "pnpm@9.15.9",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "pnpm -r --filter ./oen-digitals/artifacts/... run build && pnpm -r --filter ./oen-digitals/lib/... run build && pnpm -r --filter ./oen-digitals/scripts run build"
  },
  "dependencies": {
    "@tanstack/react-query": "^5.100.9"
  },
  "devDependencies": {
    "vite": "^8.0.12"
  }
}